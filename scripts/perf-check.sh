#!/bin/bash
# Perf budget gate — implemented in Phase 4 (idle CPU sampling + signpost durations).
# Deliberately fails until then so `make perf` cannot silently pass.
echo "perf-check: not implemented until Phase 4" >&2
exit 1
