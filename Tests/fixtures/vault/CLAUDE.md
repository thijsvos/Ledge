# register vault (fixture)

Miniature register-shaped vault used by Ledge's unit tests. Daily notes live in
`daily/YYYY-MM-DD.md` (UTC dates), the inbox is the `000*` note at the root,
templates live in `templates/`.
